<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Operadores condicionais if else</title>
</head>

<body>
    
    <?php
        
        // ( == ) -> verifica se os valores comparados são "iguais"
        // ( === ) -> verifica se os valores comparados são "iguais e do msm tipo"
        // ( != ou <> ) -> verifica se os valores comparados são "diferentes"
        // ( !== ) -> verifica se os valores comparados são "diferentes e de tipos diferentes"
        // ( < ) -> verifica se o valor da esquerda é "menor" que o valor da direita
        // ( > ) -> verifica se o valor da esquerda é "maior" que o valor da direita
        // ( <= ) -> verifica se o valor da esquerda é "menor ou igual" que o valor da direita
        // ( >= ) -> verifica se o valor da esquerda é "maior ou igual" que o valor da direita

        if(5 < 6){
            echo 'Verdadeiro';
        }else{
            echo 'Falso';
        }
    ?>

</body>
</html>